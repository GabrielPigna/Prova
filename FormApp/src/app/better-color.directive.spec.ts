import { BetterColorDirective } from './better-color.directive';

describe('BetterColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterColorDirective();
    expect(directive).toBeTruthy();
  });
});
